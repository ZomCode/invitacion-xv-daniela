---
interface Props {
    class?: string;
    img?: string;
}
---
<section class="relative isolate">
		<!-- Background -->
		<section
			class:list={["fixed inset-x-0 top-0 -z-50 h-lvh bg-[url('/bg-1.webp')] bg-cover bg-center bg-no-repeat", Astro.props.class]}
		>
        </section>
		<!-- Section -->
		<slot />
</section>